yfinance==1.5.2
pandas==3.0.5
numpy==2.5.1
xgboost==3.3.0
arch==8.0.0
scikit-learn==1.9.0
streamlit==1.60.0
plotly==6.9.0
pytz==2026.3.post1
# torch is installed separately from the CPU index to avoid pulling the multi-GB CUDA build:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
optuna==3.6.1
vaderSentiment==3.3.2
fastapi==0.115.12
uvicorn==0.34.3
# Hermes LLM agent — communicates with a locally running Ollama server
# Install Ollama first: https://ollama.com/download
ollama>=0.4.7
httpx>=0.27.0
